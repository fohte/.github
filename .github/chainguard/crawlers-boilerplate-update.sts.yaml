issuer: https://token.actions.githubusercontent.com
subject_pattern: ^repo:fohte(@[0-9]+)?/crawlers(@[0-9]+)?:ref:refs/heads/main$
claim_pattern:
  job_workflow_ref: ^fohte/crawlers/\.github/workflows/boilerplate-update\.yml@[^@]+$
repositories:
  - crawlers
permissions:
  contents: write
  pull_requests: write
  workflows: write
