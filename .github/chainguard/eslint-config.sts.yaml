issuer: https://token.actions.githubusercontent.com
subject_pattern: ^repo:fohte(@[0-9]+)?/eslint-config(@[0-9]+)?:ref:refs/heads/.+$
claim_pattern:
  job_workflow_ref: ^fohte/eslint-config/\.github/workflows/test\.yml@[^@]+$
repositories:
  - eslint-config
permissions:
  contents: write
