issuer: https://token.actions.githubusercontent.com
subject_pattern: ^repo:fohte(@[0-9]+)?/basefmt(@[0-9]+)?:ref:refs/heads/.+$
claim_pattern:
  job_workflow_ref: ^fohte/basefmt/\.github/workflows/test\.yml@[^@]+$
repositories:
  - basefmt
permissions:
  contents: write
