issuer: https://token.actions.githubusercontent.com
subject_pattern: ^repo:fohte(@[0-9]+)?/crawlers(@[0-9]+)?:ref:refs/heads/.+$
claim_pattern:
  job_workflow_ref: ^fohte/crawlers/\.github/workflows/(test|docker-publish)\.yml@[^@]+$
repositories:
  - ddr-score-manager
permissions:
  contents: read
