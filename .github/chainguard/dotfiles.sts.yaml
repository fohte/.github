issuer: https://token.actions.githubusercontent.com
subject_pattern: ^repo:fohte(@[0-9]+)?/dotfiles(@[0-9]+)?:ref:refs/heads/.+$
claim_pattern:
  job_workflow_ref: ^fohte/dotfiles/\.github/workflows/test\.yml@[^@]+$
repositories:
  - dotfiles
permissions:
  contents: write
