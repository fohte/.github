issuer: https://token.actions.githubusercontent.com
subject_pattern: ^repo:fohte/crawlers:ref:refs/heads/.+$
claim_pattern:
  job_workflow_ref: ^fohte/crawlers/\.github/workflows/test\.yml@[^@]+$
repositories:
  - crawlers
permissions:
  contents: write
