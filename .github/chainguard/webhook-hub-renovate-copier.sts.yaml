issuer: https://token.actions.githubusercontent.com
subject_pattern: ^repo:fohte(@[0-9]+)?/webhook-hub(@[0-9]+)?:pull_request$
claim_pattern:
  job_workflow_ref: ^fohte/webhook-hub/\.github/workflows/renovate-copier\.yml@[^@]+$
repositories:
  - webhook-hub
permissions:
  contents: write
